<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.system.mapper.SysUserCompanyMapper">
    <select id="selectLineByCompanyId" resultType="com.chat.system.domain.SysUserCompany">
        SELECT c.* FROM `sys_user_company` c JOIN `sys_user` u ON c.user_id= u.user_id
        WHERE u.online=1
        AND u.status=1
        AND c.company_id =#{companyId}
    </select>
</mapper> 